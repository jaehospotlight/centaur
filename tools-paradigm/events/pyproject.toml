[project]
name = "events"
version = "0.1.0"
description = "Paradigm events operations — compound ops over Google Sheets and Drive for the events team"
requires-python = ">=3.11"
dependencies = []

[tool.ai-v2]
module = "client.py"
secrets = []
